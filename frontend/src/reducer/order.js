const initialOrderState = {
  listOrder: [],
  isGetListOrder: false
}

export default function orderReducer(state = initialOrderState, action) {
  switch (action.type) {
    case "":
      break;
  }

  return state;
}
